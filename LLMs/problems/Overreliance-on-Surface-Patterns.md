Performance drops sharply when action/object names are replaced with unfamiliar or random tokens.

This reveals LLMs are relying on pattern matching and memorized associations, rather than true reasoning.